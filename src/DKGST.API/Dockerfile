FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

COPY ["src/DKGST.API/DKGST.API.csproj", "src/DKGST.API/"]
COPY ["src/DKGST.Core/DKGST.Core.csproj", "src/DKGST.Core/"]
COPY ["src/DKGST.Infrastructure/DKGST.Infrastructure.csproj", "src/DKGST.Infrastructure/"]

RUN dotnet restore "src/DKGST.API/DKGST.API.csproj"

COPY . .

RUN dotnet build "src/DKGST.API/DKGST.API.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "src/DKGST.API/DKGST.API.csproj" -c Release -o /app/publish /p:UseAppHost=false

FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY --from=publish /app/publish .

EXPOSE 5000 5001

ENTRYPOINT ["dotnet", "DKGST.API.dll"]
